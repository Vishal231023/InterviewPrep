package CoreJava.OOPS;

public interface InterfaceExample {

    //they have only abstract methods, constants(static final), and default and static methods
    //they can't have concrete methods
    //they can't have constructore
    // class can implememt interfaces and it can implements mpre then one interface
    //interfcae are used to achieve polymorphism at runtime

    public void getName();
    public  int getAge();
}
